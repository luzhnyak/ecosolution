import BurgerBtn from "../BurgerBtn/BurgerBtn";
import Logo from "../Logo/Logo";

const Header = () => {
  return (
    <div>
      <Logo />
      <BurgerBtn />
    </div>
  );
};

export default Header;
